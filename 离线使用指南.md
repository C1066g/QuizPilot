# 📱 离线使用指南 - 在没有网络的情况下使用

## 🎯 核心原理

你的 QuizPilot 应用是**完全离线可用**的应用：
- ✅ 所有数据存储在本地（localStorage）
- ✅ 不需要连接互联网
- ✅ 只需要本地 Python 服务器
- ✅ 手机和电脑可以在同一局域网内通信

---

## 🚀 快速启动步骤

### 第1步：启动服务器（电脑端）

1. **双击运行启动脚本**
   - 找到文件夹中的 `启动.bat` 文件
   - 双击运行
   - 会自动启动 Python HTTP 服务器

2. **或者手动启动**
   - 打开命令行（Win + R，输入 cmd）
   - 进入应用文件夹：`cd d:\桌面\qimofuxi`
   - 运行命令：`python -m http.server 8001`
   - 看到 `Serving HTTP on :: port 8001` 说明启动成功

### 第2步：获取电脑 IP 地址

**方法1：从命令行查看**
```
ipconfig
```
找到 `IPv4 地址`，通常是 `192.168.x.x` 或 `10.x.x.x` 的格式

**方法2：从启动脚本查看**
- 启动脚本会显示访问地址

### 第3步：手机连接

1. **确保手机和电脑在同一 WiFi 网络**
   - 手机连接到电脑所在的 WiFi
   - 不需要互联网，只需要局域网连接

2. **在手机浏览器中访问**
   - 打开手机浏览器（Chrome、Safari 等）
   - 在地址栏输入：`http://电脑IP:8001`
   - 例如：`http://<你的电脑IP>:8001`
   - 按回车，应该能看到应用界面

---

## 📊 完整的访问地址示例

假设你的电脑 IP 是 `<你的电脑IP>`：

| 页面 | 地址 |
|------|------|
| 主页 | `http://<你的电脑IP>:8001` |
| 浏览模式 | `http://<你的电脑IP>:8001/browse.html` |
| 错题集 | `http://<你的电脑IP>:8001/wrong.html` |

---

## 💾 数据同步说明

### 数据存储位置
- 所有学习进度、错题集、收藏题目都存储在**浏览器的 localStorage** 中
- 数据保存在你的设备上，不上传到云端

### 多设备同步
- **同一设备**：数据自动同步（电脑和手机浏览器各自保存）
- **不同设备**：数据不会自动同步
  - 如果需要在多个设备上同步，可以使用"导出数据"功能
  - 在统计模式中点击"📥 导出数据"下载 JSON 文件

### 数据备份
1. 在统计模式点击"📥 导出数据"
2. 会下载一个 JSON 文件
3. 妥善保存这个文件作为备份

---

## 🔧 常见问题

### Q1: 手机连不上怎么办？

**检查清单：**
1. ✅ 电脑服务器是否启动？
   - 命令行窗口是否显示 `Serving HTTP on :: port 8001`

2. ✅ 手机和电脑是否在同一 WiFi？
   - 手机 WiFi 设置中是否显示连接到正确的网络

3. ✅ IP 地址是否正确？
   - 在电脑上运行 `ipconfig` 确认 IPv4 地址
   - 手机地址栏中的 IP 是否与电脑一致

4. ✅ 防火墙是否阻止？
   - Windows 防火墙可能阻止 Python 服务器
   - 尝试关闭防火墙或添加例外

### Q2: 关闭电脑后数据会丢失吗？

**不会！** 数据存储在手机的浏览器中，关闭电脑不会影响。

但如果你清除浏览器数据或卸载浏览器，数据会丢失。建议定期导出数据备份。

### Q3: 没有 WiFi，只有热点可以吗？

**可以！** 只要手机和电脑连接到同一个热点即可。

步骤：
1. 电脑连接到手机热点
2. 获取电脑在热点中的 IP 地址
3. 手机浏览器访问该 IP 地址

### Q4: 可以在多个手机上使用吗？

**可以！** 只要都连接到同一 WiFi 或热点，都能访问。

但每个手机的学习进度是独立的（存储在各自的浏览器中）。

---

## 🎮 离线使用完整流程

```
1. 在寝室（有 WiFi）
   ↓
2. 电脑启动服务器（启动.bat）
   ↓
3. 手机连接同一 WiFi
   ↓
4. 手机浏览器访问 http://电脑IP:8001
   ↓
5. 开始学习（所有数据保存在手机浏览器）
   ↓
6. 离开寝室（关闭电脑）
   ↓
7. 手机上的数据完全保留，可以继续查看
   ↓
8. 回到寝室
   ↓
9. 重新启动电脑服务器
   ↓
10. 手机重新连接，继续学习
```

---

## 📱 手机使用建议

### 浏览器选择
- ✅ Chrome（推荐）
- ✅ Safari（iPhone）
- ✅ Firefox
- ✅ Edge
- ✅ 任何支持 HTML5 的浏览器

### 屏幕方向
- 建议使用**横屏模式**（旋转手机）
- 显示空间更大，更方便做题

### 离线查看
- 虽然服务器关闭了，但你可以在手机上查看已加载的页面
- 不能做新的题目，但可以查看错题集和统计数据

---

## 🔐 数据安全

- ✅ 所有数据存储在本地，不上传到任何服务器
- ✅ 完全隐私，不会被任何人访问
- ✅ 定期导出备份，防止数据丢失

---

## 💡 最佳实践

1. **定期导出数据**
   - 每周导出一次数据作为备份
   - 防止意外丢失

2. **使用同一设备学习**
   - 在同一个手机上学习，数据会自动保存
   - 不要频繁切换设备

3. **保持 WiFi 连接**
   - 学习时保持手机和电脑的 WiFi 连接
   - 确保数据能正常保存

4. **定期检查进度**
   - 在统计模式查看学习进度
   - 调整学习计划

---

## 📞 需要帮助？

如果遇到问题，请检查：
1. Python 是否正确安装
2. 服务器是否正常启动
3. 手机和电脑是否在同一网络
4. 防火墙是否阻止连接
5. IP 地址是否正确

祝你学习愉快！🎓
